@model ClinicAppointmentGroupProject.Controllers.AdminDashboardViewModel
@{
    ViewData["Title"] = "Admin Dashboard";
    Layout = "_Layout";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="fw-bold">👨‍💼 Admin Dashboard</h1>
        <div class="btn-group">
            <a asp-action="Users" class="btn btn-outline-primary">👥 Manage Users</a>
            <a asp-action="Appointments" class="btn btn-outline-success">📋 All Appointments</a>
            <a asp-action="CreateDoctor" class="btn btn-outline-info">➕ Add Doctor</a>
        </div>
    </div>

    <!-- Statistics Cards -->
    <div class="row mb-5">
        <div class="col-md-3 mb-3">
            <div class="card bg-primary text-white shadow">
                <div class="card-body">
                    <div class="d-flex justify-content-between">
                        <div>
                            <h4 class="card-title">@Model.TotalAppointments</h4>
                            <p class="card-text">Total Appointments</p>
                        </div>
                        <div class="align-self-center">
                            <span class="fs-1">📊</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-3 mb-3">
            <div class="card bg-success text-white shadow">
                <div class="card-body">
                    <div class="d-flex justify-content-between">
                        <div>
                            <h4 class="card-title">@Model.TotalDoctors.Count</h4>
                            <p class="card-text">Total Doctors</p>
                        </div>
                        <div class="align-self-center">
                            <span class="fs-1">👨‍⚕️</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-3 mb-3">
            <div class="card bg-info text-white shadow">
                <div class="card-body">
                    <div class="d-flex justify-content-between">
                        <div>
                            <h4 class="card-title">@Model.TotalClients.Count</h4>
                            <p class="card-text">Total Clients</p>
                        </div>
                        <div class="align-self-center">
                            <span class="fs-1">👥</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-3 mb-3">
            <div class="card bg-warning text-dark shadow">
                <div class="card-body">
                    <div class="d-flex justify-content-between">
                        <div>
                            <h4 class="card-title">@Model.PendingApprovalsCount</h4>
                            <p class="card-text">Pending Approvals</p>
                        </div>
                        <div class="align-self-center">
                            <span class="fs-1">⏳</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Quick Actions -->
    <div class="row mb-4">
        <div class="col-md-6 mb-3">
            <div class="card shadow border-warning h-100">
                <div class="card-header bg-warning text-dark">
                    <h5 class="mb-0">⏳ Pending Approvals</h5>
                </div>
                <div class="card-body">
                    @if (Model.PendingApprovalsCount > 0)
                    {
                        <div class="text-center">
                            <h3 class="text-warning">@Model.PendingApprovalsCount</h3>
                            <p class="mb-3">Users waiting for approval</p>
                            <a asp-action="PendingApprovals" class="btn btn-warning">Review Approvals</a>
                        </div>
                    }
                    else
                    {
                        <div class="text-center">
                            <h3 class="text-success">0</h3>
                            <p class="mb-3">No pending approvals</p>
                            <a asp-action="Users" class="btn btn-outline-secondary">View All Users</a>
                        </div>
                    }
                </div>
            </div>
        </div>

        <div class="col-md-6 mb-3">
            <div class="card shadow border-primary h-100">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">🚀 Quick Actions</h5>
                </div>
                <div class="card-body">
                    <div class="d-grid gap-2">
                        <a asp-action="CreateDoctor" class="btn btn-outline-primary text-start">
                            👨‍⚕️ Add New Doctor
                        </a>
                        <a asp-action="PendingApprovals" class="btn btn-outline-warning text-start">
                            ⏳ Review Pending Approvals
                        </a>
                        <a asp-action="Appointments" class="btn btn-outline-success text-start">
                            📋 View All Appointments
                        </a>
                        <a asp-action="Users" class="btn btn-outline-info text-start">
                            👥 Manage Users
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Recent Appointments -->
    <div class="card shadow">
        <div class="card-header bg-dark text-white">
            <h5 class="mb-0">📅 Recent Appointments</h5>
        </div>
        <div class="card-body">
            @if (Model.RecentAppointments.Any())
            {
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Patient</th>
                                <th>Department</th>
                                <th>Doctor</th>
                                <th>Date</th>
                                <th>Status</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var appointment in Model.RecentAppointments)
                            {
                                <tr>
                                    <td><span class="badge bg-secondary">@appointment.appointmentId</span></td>
                                    <td>@appointment.patientName</td>
                                    <td>@appointment.department</td>
                                    <td>@(appointment.Doctor?.FullName ?? "Not Assigned")</td>
                                    <td>@appointment.AppointmentDate.ToString("yyyy-MM-dd")</td>
                                    <td>
                                        <span class="badge @GetStatusBadgeClass(appointment.Status)">
                                            @appointment.Status
                                        </span>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="text-center py-4">
                    <p class="text-muted">No recent appointments found.</p>
                </div>
            }
        </div>
    </div>
</div>

@functions {
    public string GetStatusBadgeClass(ClinicAppointmentGroupProject.Models.AppointmentStatus status)
    {
        return status switch
        {
            ClinicAppointmentGroupProject.Models.AppointmentStatus.Pending => "bg-warning",
            ClinicAppointmentGroupProject.Models.AppointmentStatus.Confirmed => "bg-success",
            ClinicAppointmentGroupProject.Models.AppointmentStatus.Completed => "bg-info",
            ClinicAppointmentGroupProject.Models.AppointmentStatus.Cancelled => "bg-danger",
            _ => "bg-secondary"
        };
    }
}